require(
    ['infra/config', 'vms/post/detail'],
    function (config, vm) {

        module('post-detail viewModel');

        //asyncTest('getPost function should update post property', function () {
        //    vm.getPost(function (result) {
        //        ok(result.length > 0);
        //        ok(vm.post() != null);
        //        start();
        //    });
        //});

        //asyncTest('saveComment function should add comment', function () {
        //    // arrange
        //    vm.commenterName('commenter');
        //    vm.comment('comment');

        //    // act
        //    vm.saveComment(function () {

        //        // assert
        //        ok(vm.post().comments().length > 0);

        //        start();
        //    });
        //});

    });