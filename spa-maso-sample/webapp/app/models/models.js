define(['./referral', './document', './post'],
    function (Referral, Document, Post) {

        return {
            Referral:Referral,
            Document: Document,
            Post: Post
        };
    }
);